import ReactDOM from "react-dom/client";
import App2 from "./dnd-with-library";

const root = ReactDOM.createRoot(
  document.getElementById("root") as HTMLElement
);
root.render(
  <>
    <App2 />
  </>
);
